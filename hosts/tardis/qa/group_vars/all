site_color: "qa"
site_role: "{{ ansible_hostname.split('-')[0] }}"
site_number: "{{ ansible_hostname.split('-')[2] }}"
site_project: "{{ ansible_hostname.split('-')[0][0:3] }}"
site_role_name: "{{ ansible_hostname.split('-')[0][3:] }}"
site_region: "{{ 'e1' if ansible_hostname.split('-')[1] in ['e1a','e1b','e1c','e1d'] else 'w2' if ansible_hostname.split('-')[1]  in ['w2a','w2b','w2c'] else 'c1' if ansible_hostname.split('-')[1] in ['c1a','c1b','c1c'] else 'w1' if ansible_hostname.split('-')[1] in ['w1a','w1b','w1c'] else ansible_hostname.split('-')[1] }}"
site_region_name: "{{'east' if ansible_hostname.split('-')[1] in ['e1a','e1b','e1c','e1d'] else 'west' if ansible_hostname.split('-')[1] in ['w2a','w2b','w2c'] else ansible_hostname.split('-')[1] }}"
site_cregion: "{{'EAST' if site_region == 'w2' else 'WEST' if site_region == 'e1' else 'HOA' if site_region == 'poa' else 'POA' if site_region == 'hoa'}}"
site_country: "{{ 'EU' if ansible_hostname.split('-')[1] in ['c1a','c1b','c1c','w1a','w1b','w1c'] or site_region in ['c1', 'w1'] else 'US' }}"
op5_gather_facts: "yes"
